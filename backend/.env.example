# ── FRONTEND (set these in Vercel dashboard) ──────────────────────────────────
# Gemini AI key from https://aistudio.google.com
GEMINI_API_KEY="your_gemini_api_key_here"

# Your deployed Render backend URL (update after Render deploy)
ML_BACKEND_URL="https://raktcare-api.onrender.com"

# ── BACKEND (set these in Render dashboard) ───────────────────────────────────
# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS="https://your-app.vercel.app,http://localhost:3000"
